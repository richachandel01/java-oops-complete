# 20 - Immutable Objects in Java

An immutable object is an object whose state cannot be changed after creation.

## Famous Example

`String` is immutable.

```java
String name = "Java";

name.concat(" Programming");

System.out.println(name);