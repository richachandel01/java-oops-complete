# 21 - Association in Java

Association represents a relationship between two independent objects.

It describes a "uses-a" or "knows-a" relationship.

## Example

A Student knows a Teacher.

A Doctor treats a Patient.

Both objects can exist independently.

```text
Student -------- Teacher
   knows